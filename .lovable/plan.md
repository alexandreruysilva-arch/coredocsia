Ajuste a política DELETE de `ai_usage_logs` para restringir exclusão de logs de auditoria a administradores da organização. Reveja a migração mais recente de faturamento IA (colunas `ai_cost_per_file`, `ai_price_base_threshold`, `ai_price_tier_step`, `ai_price_tier_increment` em `organizations`) e confirme se há GRANTs adequados para `authenticated` e `service_role` nessa tabela. Preserve as demais políticas existentes.